# LazySorter
The Lazy Sorter is a simple Python program designed to quickly pick all files in the parent folder and move them to new folders depending on the nature of the files. To use it, drop the LazySorter folder inside the directory to sort out.
